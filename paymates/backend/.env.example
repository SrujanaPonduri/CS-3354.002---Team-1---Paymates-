FRONTEND_BASE_URL=http://localhost:3000/

# Optional: include token in signup/login JSON for dev flows
MAGIC_LINK_RETURN_TOKEN=false

# SMTP config (leave SMTP_HOST empty for console-mode magic links)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
EMAIL_FROM=
